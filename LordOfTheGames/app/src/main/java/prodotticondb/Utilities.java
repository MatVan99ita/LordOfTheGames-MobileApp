package prodotticondb;

import android.os.Bundle;

import androidx.appcompat.app.AppCompatActivity;
import androidx.fragment.app.Fragment;
import androidx.fragment.app.FragmentTransaction;

public class Utilities {/*
    public void insertFragment(
            AppCompatActivity activity,
            Fragment fragment,
            int fragmentContainerViewId,
            String tag,
            Bundle bundle ){


        FragmentTransaction transaction = activity.getSupportFragmentManager().beginTransaction();

        fragment.setArguments(bundle);

        transaction.replace(fragmentContainerViewId, fragment, tag);

        if(!(fragment instanceof EditFragment) && !(fragment instanceof DetailsFragment)) transaction.addToBackStack(tag);

        transaction.commit();
    }
*/
}
